# Prometheus Node Exporter

Quick reference for Prometheus Node Exporter, a tool or engineering platform mentioned in the `guesant/hl-infrastructure` documentation.

## Repository mapping

- Catalog id: `prometheus-node-exporter`
- Configuration namespace: `tools/prometheus-node-exporter/`
- Discovery source: `guesant/hl-infrastructure/docs/`
- Current phase: inventory and reference

## Entry point

This project does not assume a universal standalone CLI for Prometheus Node Exporter. Inspect it through the package manager, deployment manifest, Helm chart, provider, service API, or the control plane that owns it.

## Keep here

Add stable, reusable reference material only:

- version and installation checks;
- read-only inspection commands;
- validation or lint commands;
- safe troubleshooting commands;
- links to presets, profiles, recipes, or policies once they are extracted.

Project-specific procedures belong in the project that owns the deployment.
