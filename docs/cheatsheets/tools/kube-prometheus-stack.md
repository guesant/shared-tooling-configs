# Kube Prometheus Stack

Quick reference for Kube Prometheus Stack, a tool or engineering platform mentioned in the `guesant/hl-infrastructure` documentation.

## Repository mapping

- Catalog id: `kube-prometheus-stack`
- Configuration namespace: `tools/kube-prometheus-stack/`
- Discovery source: `guesant/hl-infrastructure/docs/`
- Current phase: inventory and reference

## Entry point

This project does not assume a universal standalone CLI for Kube Prometheus Stack. Inspect it through the package manager, deployment manifest, Helm chart, provider, service API, or the control plane that owns it.

## Keep here

Add stable, reusable reference material only:

- version and installation checks;
- read-only inspection commands;
- validation or lint commands;
- safe troubleshooting commands;
- links to presets, profiles, recipes, or policies once they are extracted.

Project-specific procedures belong in the project that owns the deployment.
