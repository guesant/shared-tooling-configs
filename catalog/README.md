# Catalog

The catalog classifies tooling independently from the physical repository tree.

Initial dimensions are capabilities, languages, targets, and lifecycle stages. Tool manifests may later reference identifiers from these taxonomies.
