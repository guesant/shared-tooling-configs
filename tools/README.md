# Tools

This directory is the canonical location for tool-specific reusable configurations.

During the initial repository inventory, tool directories contain only `.gitkeep`. Concrete configurations and presets are added only after comparing their usage across source repositories and identifying a reusable baseline.

A tool may later contain multiple presets for different languages, frameworks, targets, or strictness levels without being duplicated elsewhere in the repository.
