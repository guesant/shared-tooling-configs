# Profiles

Profiles describe cross-tool levels of engineering enforcement.

Expected initial profiles are `minimal`, `recommended`, and `strict`. Their exact requirements should be defined after the repository inventory identifies broadly applicable controls.

Profiles express expectations and select compatible presets rather than duplicate tool configuration.
